# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# mistlib's own git clone cache used by scripts/fetch-mistlib.mjs; the
# built output it produces (src/vendor/mistlib) IS committed.
.mistlib-src

.env

# Claude Code local runtime state (session locks, worktrees, personal overrides)
.claude/scheduled_tasks.lock
.claude/worktrees/
.claude/settings.local.json

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.omc
